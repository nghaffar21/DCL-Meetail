import { hud } from "@dcl/builder-hud"

export function HudBoxTest(): void {

// var box = new Entity("Box")
// box.addComponent(new BoxShape())
// box.addComponent(new Transform({position: new Vector3(4,0,8), rotation: Quaternion.Euler(0,0,0), scale: new Vector3(1,1,1)}))
// engine.addEntity(box)
// hud.attachToEntity(box)
}
